# Auto-generated by fullend gen — do not edit.
# Run: hurl --test --variable host=http://localhost:8080 smoke.hurl

# ===== Auth =====

# Register
POST {{host}}/auth/register
Content-Type: application/json
{
  "email": "test_string",
  "name": "test_string",
  "password": "Password1234!",
  "role": "test_string"
}

HTTP 200
[Asserts]
jsonpath "$.user" exists

# Login
POST {{host}}/auth/login
Content-Type: application/json
{
  "email": "test_string",
  "password": "Password1234!"
}

HTTP 200
[Captures]
token: jsonpath "$.token"
[Asserts]
jsonpath "$.token" exists


# ===== Gigs =====

# CreateGig
POST {{host}}/gigs
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "budget": 1,
  "description": "test_string",
  "title": "test_string"
}

HTTP 200
[Asserts]
jsonpath "$.gig" exists

# PublishGig
PUT {{host}}/gigs/{{id}}/publish
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists

# SubmitProposal
POST {{host}}/gigs/{{id}}/proposals
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "bid_amount": 10000
}

HTTP 200
[Asserts]
jsonpath "$.proposal" exists


# ===== Proposals =====

# AcceptProposal
POST {{host}}/proposals/{{id}}/accept
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists
jsonpath "$.proposal" exists


# ===== Gigs =====

# SubmitWork
POST {{host}}/gigs/{{id}}/submit-work
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists

# ApproveWork
POST {{host}}/gigs/{{id}}/approve
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists

# RaiseDispute
POST {{host}}/gigs/{{id}}/dispute
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists


# ===== Proposals =====

# RejectProposal
POST {{host}}/proposals/{{id}}/reject
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.proposal" exists


# ===== Gigs =====

# ListGigs
GET {{host}}/gigs?limit=2&sort=created_at&direction=desc&status=test_string

HTTP 200
[Asserts]
jsonpath "$.items" isCollection
jsonpath "$.total" exists

# GetGig
GET {{host}}/gigs/{{id}}

HTTP 200
[Asserts]
jsonpath "$.gig" exists

